# Run Android Lint

Run lint checks on the project using Gradle.

## Steps
1. Run `gradle lint` to perform static analysis
2. Parse the lint report at `app/build/reports/lint-results-debug.html`
3. Summarize any warnings or errors found
4. Suggest fixes for critical issues
